# Windows App

WinUI 3 / Windows App SDK C# skeleton. Open in Visual Studio with the Windows App SDK installed. Locales are limited to `en-AU`, `zh-Hans`, and `en-XA`. This scaffold intentionally does not use WPF or WinForms.
